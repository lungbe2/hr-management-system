import React from 'react';

const DocumentManager = ({ token, user }) => {
  return (
    <div className="dashboard-container">
      <h2>📄 Document Management</h2>
      <div className="attendance-section">
        <p style={{ color: 'var(--text-secondary)' }}>Document management feature coming soon...</p>
        <p style={{ color: 'var(--text-secondary)', fontSize: '0.9rem' }}>You'll be able to upload, categorize, and search documents.</p>
      </div>
    </div>
  );
};

export default DocumentManager;
